\section{Discussion}
\todoMissing{write discussion}

%	Quality of results
%	Explanation of good/bad results
%		Kernel Function: are our kernels good? Why do we use those kernels?
%		kernel functions:
%			5, 20 + 20, 50 + 100, 200 + z500, 1000
%			5, 20 + 20, 50 + 100, 200 + 500, 1000
%			5, 20 + 20, 50 + 100, 200 + 500, 500
%			5, 20 + 20, 50 + 100, 200 + 20, 20
%		kernel model?
%		fit of training data?
%		trained model?
%		fit of final bone reconstruction
		
%	Improvements inside the scope of the project
%	Find the best kernel
%	Pick a better sigma for the kernel?
%	Improvements outside the scope of the project
%	Use better procedures (bayes) instead of icp